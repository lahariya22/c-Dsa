#include<stdio.h>
int main()
{
	// in var name no keyword use 
//	int Char = 23;
int char =74; // c is a case senstive language in c we don't use keyword but in capital we use it ...
printf("%d", char );

	printf("%d", Char );
	
	
	return 0;
	
}